'use strict';

describe('pr0texter.pr0texter module', function() {

  beforeEach(module('pr0texter.pr0texter'));

  describe('pr0texter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pr0texterCtrl = $controller('Pr0texterCtrl');
      expect(pr0texterCtrl).toBeDefined();
    }));

  });
});